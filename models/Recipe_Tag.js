module.exports = (sequelize, DataTypes) => {
    const Recipe_Tag = sequelize.define("Recipe_Tag", {
        // category: {
        //     type: DataTypes.STRING,
        //     allowNull: true
        // }
    });
return Recipe_Tag;
};